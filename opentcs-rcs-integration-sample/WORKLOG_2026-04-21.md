# WORKLOG 2026-04-21

## 1. 今日目标

1. 在现有 demo-wcs + openTCS LIVE 基础上，落地 Sprint 1 的第一批后端能力。
2. 先做文档，再做实现，再做真实平台验证。

## 2. 今日完成

### 2.1 Sprint 1 核心实现（已落地）

1. 新增 `biz_task_no` 维度任务聚合模型与存储：
   - `core/task/WcsTaskRecord`
   - `core/task/TaskStore`
   - `core/task/InMemoryTaskStore`
   - `core/task/FileTaskStore`
   - `core/task/WcsTaskType`
   - `core/task/WcsTaskStatus`

2. 新增 WMS -> WCS 任务接口：
   - `POST /api/v1/wcs/inbound/tasks`
   - `POST /api/v1/wcs/outbound/tasks`
   - `GET /api/v1/wcs/tasks/{biz_task_no}`
   - `POST /api/v1/wcs/tasks/{biz_task_no}/cancel`

3. 新增服务与处理器：
   - `api/wcs/WcsTaskService`
   - `api/wcs/WcsTaskHttpHandlers`
   - DTO: `CreateWcsTaskReq/CreateWcsTaskResp/QueryWcsTaskResp/CancelWcsTaskResp`

4. 异常映射与错误码补齐：
   - `RCS-4004` 资源不存在
   - `RCS-4090` 状态机冲突
   - 新增异常：`ResourceNotFoundException`、`TaskStateConflictException`

5. demo 页面增强：
   - 在 `/demo/wcs` 增加入库/出库任务创建、任务查询、任务取消可点击操作。

### 2.2 文档更新

1. 新增 `WMS_WCS_TASK_API_V1.md`（Sprint 1 当前接口说明与示例）。
2. 已有迭代文档保持有效：
   - `ITERATION_REQUIREMENTS_V1.md`
   - `ITERATION_TASK_BREAKDOWN_V1.md`

### 2.3 测试与验证

1. 自动化测试：
   - 执行 `:opentcs-rcs-integration-sample:test`
   - 结果：`BUILD SUCCESSFUL`

2. 新增测试：
   - `api/wcs/WcsTaskServiceTest`
   - `core/task/FileTaskStoreTest`
   - `RcsIntegrationApplicationTest` 新增任务接口集成用例

3. 真实平台联调（openTCS Web API `http://127.0.0.1:55200`）：
   - `GET /v1/kernel/version` 可用
   - LIVE 模式下调用 `POST /api/v1/wcs/inbound/tasks` 返回 `code=0`
   - `GET /v1/transportOrders` 数量从 `8` 到 `9`（`delta=1`），确认真实创建 Transport Order
   - 注入 `FINISHED` 事件后，任务查询状态为 `WAIT_PLC`（入库流程预期）

## 3. 当前状态结论

1. Sprint 1 的 `S1-T1 ~ S1-T4` 已有可运行基础版本。
2. demo-wcs 已不只是 mission 维度，已支持 `biz_task_no` 任务维度点测。
3. 与真实 openTCS 的订单创建已验证通过。

## 4. 明日建议起手顺序

1. `S1-T5`：实现 `WCS -> WMS` 入/出库结果回传（复用 outbox/retry 机制）。
2. `S1-T6`：继续统一错误码与异常边界（覆盖新增接口的更多失败分支）。
3. 增加一键 LIVE E2E 脚本（覆盖新任务接口链路），形成可重复验收基线。

## 5. 备注

1. `http://127.0.0.1:55200/` 返回 `Endpoint GET / not found` 属于正常现象；应访问 `/v1/...` 路径。
2. 今日联调过程中验证了这一点，不属于故障。
